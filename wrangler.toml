# Cloudflare Pages Configuration - FREE TIER ONLY
# This is a static site - NO Workers (keeps you on free tier)
# Do NOT add serverless functionality or paid add-ons

name = "harmanjeet-portfolio"
account_id = ""  # Add your Cloudflare Account ID

# Pages deployment settings
pages_build_output_dir = "./out"

# Build configuration
build = { command = "npm run build", cwd = "." }
